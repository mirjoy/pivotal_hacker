<h1>All Projects</h1>
<ul>
<% @projects.each do |proj| %>
  <li><%= link_to proj.title, project_path(proj) %></li>
<% end %>
</ul>

<%= link_to "Create a New Board", new_project_path, class: "btn btn-success" %>
